<nav class="cluster" style="--pad: var(--s0); --flow-space: var(--s-1);">
  <ul class="cluster" style="--pad: var(--s-1);">
    {%- for item in navigation %}
      <li>
        <a
        class="{%- if (item.link | slugify) === (page.url | slugify) %}active{%-endif-%}"
        href={{item.link}}>{{item.text}}</a>
      </li>
    {% endfor %}
  </ul>
  <div class="nav__buttons cluster">
    <a class="button" rel="noreferrer" href="https://donate.stripe.com/eVa9AZ1XX9pq1MceUU">Donate</a>
    <a class="button" href="/contact">Volunteer</a>
  </div>
</nav>